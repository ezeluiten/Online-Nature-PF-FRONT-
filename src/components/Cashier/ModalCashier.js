import React from 'react'
import {ModalContainer} from "./ModalCashierStyles"

export const ModalCashier = ({isOpen}) => {
  console.log("🚀 ~ file: ModalCashier.js:5 ~ ModalCashier ~ isOpen", isOpen)
  return (
    <ModalContainer>
        
    </ModalContainer>
  )
}
